import React from "react";
import Projects from "../components/Projects";

const index = () => {
   return (
      <React.Fragment>
         <Projects/>
      </React.Fragment>
   );
};

export default index;
