import React from "react";
import MainSection from "../components/MainSection";

const index = () => {
   return (
      <React.Fragment>
         <MainSection />
      </React.Fragment>
   );
};

export default index;
